Given /^The User has installed The Game of Assault$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The User first runs the Program$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^The Program should make sure the User has all dependant Gems installed$/ do
  pending # express the regexp above with the code you wish you had
end
